---
ansible-navigator:
  app: welcome
  execution-environment:
    container-engine: podman
    image: {{ ansible_image }}
    pull:
      policy: missing
  logging:
    level: debug
  editor:
    command: code-server {filename}
    console: false
